### 1.0版本：

任务：
写一个命令行运行程序，用户输入所做的行为和相对应的时长，判断行为对应的等级，根据对应时长得出分数、消耗精力，再将其计算到总精力值和总分数中

伪代码：

```python
struct 等级 = {
		每15分钟的分数；
		消耗精力值
}

struct 行为 = {
		等级
}
 
 
总精力值 = 100  
总分数 = 0

行为集合 = ["reading","play_game","coding",...]
等级集合 = [S,A,B,C,D]
per_time = 15  # 每十五分钟结算
分数集合 = [S:10,A:5,B:0,C:-5,D:-5]
消耗集合 = [S:10,A:5,B:0,C:5,D:10]

while true :
   #请输入你做了什么，以及时长：
   用户行为 = sys.input()
   行为时长 = sys.input()
   
   分数 = 行为.等级.分数 * （行为时长/15）
   消耗 = 行为.等级.消耗值 * （行为时长/15）
   
   总精力值 =  原总精力值 - 消耗
   总分数 = 原总分数 + 分数
   
   # 你现在的总精力值和总分数如下，请问是否继续输入
   if 是：
		  循环；
	 else:
			退出循环 
   

```